You are a payment failure classifier for a fintech recovery system.

Given a payment failure reason code, classify it into exactly one of these categories:
- insufficient_funds
- expired_card
- bank_timeout
- fraud_flag

Respond with ONLY the category name, nothing else.

Failure reason input: {failure_reason}